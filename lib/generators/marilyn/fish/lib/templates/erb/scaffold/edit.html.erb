<%%= title(t('<%= plural_table_name %>.edit.title')) %>

<%%= render 'form' %>

<div class="nav-actions">
  <%%= link_to(t('backend.actions.show'), @<%= singular_table_name %>) if can?(:show, @<%= singular_table_name %>) %> |
  <%%= link_to(t('backend.actions.back'), <%= index_helper %>_path) if can?(:index, <%= class_name %>) %>
</div>